{
  "file": "packages/tui/src/mini/stream-v2.transport.ts",
  "replacements": {
    "`${output.output}${incomplete ? `${output.output.endsWith(\"\\n\") || !output.output ? \"\" : \"\\n\"}[output truncated]` : \"\"}`": "`${output.output}${incomplete ? `${output.output.endsWith(\"\\n\") || !output.output ? \"\" : \"\\n\"}[输出已截断]` : \"\"}`",
    "text: `→ Skill \"${name}\"`": "text: `→ 技能 \"${name}\"`",
    "`Thinking: ${text}`": "`思考: ${text}`",
    "text: \"running shell\"": "text: \"命令行运行中\"",
    "`Thinking: ${item.text}`": "`思考: ${item.text}`",
    "`Thinking: ${event.data.delta}`": "`思考: ${event.data.delta}`",
    "`Thinking: ${event.data.text}`": "`思考: ${event.data.text}`",
    "text: \"Compaction\"": "text: \"压缩\""
  }
}
